# 数据源
spring:
  datasource:
    username: hc
    password: Hc_123456
    url: jdbc:mysql://139.199.175.93:3306/study?serverTimezone=GMT%2B8&characterEncoding=utf8&useSSL=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: seata-b
  cloud:
    nacos:
      discovery:
        server-addr: 139.199.175.93:8848
        username: nacos
        password: nacos
# mybatis-plus 相关配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  mapper-locations: mapper/*.xml
server:
  port: 8084
seata:
  enabled: true
  application-id: seata-b
  registry:
    # 配置seata的注册中心， 告诉seata client 怎么去访问seata server(TC)
    type: nacos
    nacos:
      server-addr: 139.199.175.93:8848  # seata server 所在的nacos服务地址
      application: seata-server-window    # seata server 的服务名seata-server ，如果没有修改可以不配
      username: nacos
      password: nacos
      group: SEATA_GROUP          # seata server 所在的组，默认就是SEATA_GROUP，没有改也可以不配
  config:
    type: nacos
    nacos:
      server-addr: 139.199.175.93:8848
      username: nacos
      password: nacos
      group: SEATA_GROUP
  tx-service-group: default_tx_group
  enable-auto-data-source-proxy: false
